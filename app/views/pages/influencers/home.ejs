<!DOCTYPE html>
<html>
  <head>
    <% include ../../partials/head %>
  </head>
  <body>
      <header>
        <% include ../../partials/header %>
      </header>
      <section id = "homeSection">
    <main>
        <div class="container-fluid">
          <%if (posts.length === 0) { %>
            <h3 class="text-center">No posts created yet.</h3>
          <% } else { %>
            <table class="table table-borderless">
              <thead>
                <tr>
                  <th scope="col">Thumbnail</th>
                  <th scope="col">Brand</th>
                  <th scope="col">Date Posted</th>
                  <th scope="col">Actions</th>
                </tr>
              </thead>
              <tbody>
                  <% posts.forEach(function(post) { %>
                    <tr>
                      <td><img src="<%= post.img_urls[0] %>" alt="<%= post.title %> Thumbnail" class="img-thumbnail" style="max-width: 200px;"></td>
                      <th scope="row"><a href="posts/<%= post.id %>"><%= post.brand %></a></th>
                      <td><%= moment(post.date).format('M/D/YYYY') %></td>
                      <td>
                        <div class="btn-group btn-group-sm" role="group" aria-label="actions">
                          <a href="/influencers/posts/<%= post.id %>/edit"><button type="button" class="btn btn-secondary">Edit</button></a>
                          <button type="button" class="btn btn-danger">Delete</button>
                        </div>
                      </td>
                    </tr>
                  <% }); %>
              </tbody>
            </table>
          <% } %>
        </div>
    </main>
      </section>
      
    <footer>
      <% include ../../partials/footer %>
    </footer>
  </body>
</html>
